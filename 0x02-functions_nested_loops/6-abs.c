#include "main.h"
/**
 * _abs - Return the absolute value of a number.
 *
 * @n: n is the input number
 *
 * Return: positive value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
